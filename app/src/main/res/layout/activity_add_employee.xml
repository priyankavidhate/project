<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_sign_up_form"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.rohitvyavahare.project.SignUpForm">

    <EditText
        android:id="@+id/EditEmployeeId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/email_id"
        android:layout_marginTop="20dp"
        android:inputType="textEmailAddress">
    </EditText>

    <EditText
        android:id="@+id/EditEmployeeRole"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/employee_role"
        android:layout_marginTop="20dp"
        android:inputType="textNoSuggestions">
    </EditText>

    <CheckBox
        android:id="@+id/EmployeeBand"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Can add employee and place an order"
        android:layout_above="@+id/button"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/AddEmployee"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Submit"
        android:layout_marginTop="20dp"
        android:textColor="#ffffff"
        android:background="#00bfff"/>

</TableLayout>